HERMES_COMMAND=hermes
BRIDGE_PORT=8000
PROCESS_TIMEOUT=3600
# WIKI_ROOT=~/wiki   # optional default for scratch-pad Save As.
# A UI setting persisted at ~/.echo/wiki_config.json takes precedence if present.
